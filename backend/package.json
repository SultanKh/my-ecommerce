{
  "name": "server",
  "version": "1.0.0",
  "description": "for lottery only",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sultan",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5"
  }
}
